{
  "name": "prisma-orm-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch src --exec ts-node src/index.ts",
    "pm2-start": "pm2 describe dev_server > /dev/null || pm2 start dist/index.js --name dev_server",
    "pm2-restart": "pm2 restart dev_server",
    "deploy": "npm run build && (pm2 describe dev_server > /dev/null && pm2 restart dev_server || pm2 start dist/index.js --name dev_server)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.1",
    "nodemon": "^3.1.11",
    "prisma": "^7.1.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.1.0",
    "@prisma/client": "^7.1.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  }
}
